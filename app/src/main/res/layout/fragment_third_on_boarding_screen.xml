<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.example.graduation_project.ui.onboarding.ThirdOnBoardingScreenFragment ">

        <!-- TODO: Update blank fragment layout -->
        <ImageView
            android:id="@+id/imgOnboard"
            android:layout_width="100dp"
            android:layout_height="150dp"
            android:layout_gravity="center"
            android:src="@drawable/quran"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            />
        <TextView
            android:id="@+id/tvOnboard"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintRight_toRightOf="@id/imgOnboard"
            app:layout_constraintLeft_toLeftOf="@id/imgOnboard"
            android:text="امحِ اميتك الإسلامية"
            android:textAlignment="center"
            android:textSize="24sp"
            android:gravity="center"
            android:textStyle="bold|italic"
            android:layout_margin="10dp"
            app:layout_constraintTop_toBottomOf="@id/imgOnboard"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>